# screens

Top level route screens. Each screen maps to one path in `router.tsx`.

- `CommandCenterScreen.tsx` — Main operator view. Renders the command center shell.
- `AuthCallbackScreen.tsx` — OAuth redirect handler. Signs in and sends the user back to `/`.
