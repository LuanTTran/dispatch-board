** Map uses CARTO Positron for the light/dark tiles. **

** Viewport locked to Chicagoland, per the geospatial data in the seed datas. **

** Default zoom @ 10 , range 9-14 ** .

** Markers:** site pins from `useMapSites()` (OPEN WorkOrder + CustomerSite join), tech pins from `useMapTechnicians()` (Chicago-hub Technician pool + assignment-derived jobs-left). Multi-WO site popups come from ontology grouping by `siteId`.

